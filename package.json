{
  "name": "draw-io-skill",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Best-of-both-worlds draw.io skill assets for Codex and Claude Code, with export helpers and SVG lint tooling.",
  "scripts": {
    "check:node": "node --check scripts/check-drawio-svg-overlaps.mjs",
    "check:export-node": "node --check scripts/export-drawio.mjs",
    "check:fixture": "node scripts/check-drawio-svg-overlaps.mjs fixtures/basic/basic.drawio.svg",
    "check": "npm run check:node && npm run check:export-node && npm run check:fixture"
  }
}
